# Platform Roadmap

## Purpose

This document summarizes the platform's phased implementation roadmap.

## Current Status

The project is currently in `Phase 2 — read-only product foundation`.

Phase 2 to date has established:

- repository structure
- service boundaries
- topology skeleton
- Prometheus and Grafana observability (provisioned dashboards; bounded real metrics where implemented)
- bounded live read paths for backend, collector, and frontend; Postgres for the current read-side slice
- normalized model and schema scaffolding
- core architecture documents
- a useful read-only API, WebUI, and observability slice
- bounded persistence for inventory, topology, and policy snapshots plus sync-run history
- bounded workflow-history and audit-history visibility derived from persisted sync activity
- sharper capability semantics plus a stricter descriptive dry-run-planning-readiness assessment
- stronger platform, topology, and SR policy dashboards with bounded freshness, agreement, and evidence-gap cues where real metrics exist
- a first bounded runtime-hardening slice with repo-built local images, startup-contract validation for the most important stateful services, and small post-deploy verification scripts for the core runtime and ODL auth path
- accepted week 13 identity consolidation, with explicit persisted and response-level anchors now exposed where real records exist and no immediate capability item-ID follow-on justified for the current bounded product slice
- bounded platform-status `read_paths` coverage, freshness, degraded-scope, and policy detail-ready posture now carried through the backend, surfaced in the product, and mirrored numerically in the platform overview dashboard
- accepted week 14 topology truth-depth work, with explicit endpoint-pairing posture and paired-versus-single-sided inferred-link counts now carried end to end through collector delivery, backend-owned topology and platform-status contracts, primary WebUI trust cues, Grafana observability, targeted tests, and the live verifier while preserving explicit partial-truth semantics
- the strict policy truth-depth review is now also complete, and it confirms that the live collector path now yields a narrow but real detail-ready policy slice: the current stack exposes `detail_mode=static_policies_when_present`, `empty_reason=none`, `detail_ready_target_count=4`, and 4 normalized live `static_local` policy records, while 30 targets still remain explicitly `no_policies_observed`
- stronger automated verification, with backend and collector tests plus `verify-core-runtime` now checking the accepted week 13 identity, read-path, and week 14 topology-coverage signals directly
- week 16 operational checkpoint: topology coverage history persistence and product consumption, policy source-readiness history persistence and product consumption, workflow-history and audit-history baseline summaries (backend and frontend), same-workspace restart drill with preserved-baseline verifier checks
- week 18 closure (operator truth, history honesty, observability scaffolding): cross-file doc alignment with **devices/inventory** history verifier behavior (same **`history.recent_snapshots`** gate and notices pattern as topology/policy); **workflow/audit** inventory snapshot and comparison trust language in **app-web** plus **`test_app.py`** JSON contracts for presence and honest-null comparison; **Readiness** in **app-web** and **`platform-overview`** aligned on **evaluation sample** versus **persisted snapshot** age plus **mirror** bargauges for status, planning, blockers, and evidence coverage tied to **`platform_app_api_readiness_*`** metrics—observability-only; **change-validation** Grafana family as an explicit **markdown-only** non-claims scaffold (no fake PromQL); **vendor** Grafana overview (**`vendor-overview`**) with **real** bounded **`platform_gnmi_collector_*`** plus **`app-api`** collector-boundary **duration**, **timeout budget**, and **posture** (**Nokia-first**, no multi-vendor parity claims); **`roadmap.md`**, **`production-readiness-assessment.md`**, **`03-CURRENT-STATUS.md`**, **`deployment-runbook.md`**, and **`data-flows.md`** rolled forward as one bounded narrative with **no phase change** and **no** change to **`conditionally_ready_with_explicit_limits`**
- week **19** inventory-history slice (per **`ADR-0001`** default): richer **`/api/v1/devices`** **`history`** (anchors, comparisons, bounded **`change_preview`**), **WebUI** and entry-page cues, expanded **`verify-core-runtime`** inventory **`history`** assertions, **`app-api`** **`/metrics`** **`inventory_snapshots`** table gauges (**`platform_app_api_inventory_snapshots_persisted_total`**, **`platform_app_api_inventory_snapshot_latest_persisted_at_seconds`**) with **`dashboards.md`** / **platform-overview** and cross-doc (**`deployment-runbook.md`**, **`data-flows.md`**, **`production-readiness-assessment.md`**) product-versus-observability alignment—still **read-only**, **no** workflow or validation claims
- **week 20** policy-history slice (parallel narrative to week **19** inventory): richer **`/api/v1/policies`** **`history`** (anchors, nested source-readiness, comparison when two snapshots exist), **WebUI** and verifier hardening, **`app-api`** **`/metrics`** **`policy_snapshots`** table gauges (**`platform_app_api_policy_snapshots_persisted_total`**, **`platform_app_api_policy_snapshot_latest_persisted_at_seconds`**) with **`dashboards.md`**, **SR policy** / **platform-overview**, and cross-doc (**`deployment-runbook.md`**, **`data-flows.md`**, **`production-readiness-assessment.md`**, this file) product-versus-observability alignment—**static_local**-justified policy depth explicit; still **read-only**, **no** workflow or validation claims
- **Week 19 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **19** as delivered bounded value; **`conditionally_ready_with_explicit_limits`** unchanged; full live **`verify-core-runtime`** pass including new **`/metrics`** families requires **rebuilt** **`app-api`** images and redeploy (documented path: **`build-images.sh`** → **`clab deploy`** → **`verify-core-runtime.sh`** → **`verify-odl-auth.sh`**)
- **Week 20 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **20** policy-history deepening as delivered bounded value; **`conditionally_ready_with_explicit_limits`** unchanged; repository **`pytest`** covers expanded contracts and persistence; full live **`verify-core-runtime`** pass including policy **`history`** branches and **`policy_snapshots`** **`/metrics`** families requires **rebuilt** **`app-api`** images and redeploy on a lab stack (same documented path as week **19**)
- **Week 20 closure (Friday task 02):** **`policy-truth-depth-review.md`** adds **Post–Week 20 scheduling note (policy truth-depth checkpoint)**—week **20** closed **history and observability alignment** for the bounded policy envelope, not new live proof beyond **`static_local`**; default next step remains **`ADR-0001`** **Priority 2** evidence-first reassessment
- **Week 21 (Phase 2 doc drift cleanup):** cross-file wording alignment (`platform/README.md`, `platform/docs/*`, service READMEs where needed) so bounded capability, limitation, and observability boundaries stay consistent—**no phase change**, **no** new capability claims beyond evidence, **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 21 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **21** as bounded operator-truth, read-side contract alignment, verifier structural checks, vendor/collector-boundary/ODL wording, and cross-file documentation honesty—**no phase change**, **`conditionally_ready_with_explicit_limits`** unchanged; stacks predating verifier or image changes still need **rebuild → redeploy → verify** to observe the latest checks at runtime (same documented path as prior weeks)
- **Week 21 closure (Friday task 02):** **`post-week-21-bounded-phase2-recommendation.md`** states the post–week **21** **evidence-first** scheduling default, narrow follow-on categories when proof exists, and **anti-recommendations** against topology/policy/vendor expansion by momentum—**no** new default implementation lane, **no** phase transition
- **Week 22 (read-side query ergonomics):** bounded optional query parameters on **`GET /api/v1/devices`**, **`GET /api/v1/policies`**, **`GET /api/v1/workflow-history`**, and **`GET /api/v1/audit-history`** (`limit`, `history_recent_limit`, `sync_runs_limit`, `readiness_snapshot_history_limit` on audit only) with honest **`read_side_query`** echo metadata—**usability and payload sizing only**, not new truth domains; WebUI keeps **`view`** and the same parameter names in the page URL for shareable read-only views; workflow-history and audit-history detail panels add **read-only** navigation to related product surfaces (Devices, Topology, Policies, Readiness) via existing **`view=`** URL patterns—**not** workflow execution or approvals; **`verify-core-runtime.sh`** includes structural checks that optional bounded query strings on history endpoints still return request echo fields in **`read_side_query`**; repository **`pytest`** / **`vitest`** cover validation and echo—**no phase change**, **no** workflow or validation-engine claims, **`conditionally_ready_with_explicit_limits`** unchanged (see **`data-flows.md`**, **`deployment-runbook.md`**)
- **Week 22 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **22** as closed bounded read-side query ergonomics, history drilldown navigation, regression coverage, and cross-doc alignment; **`conditionally_ready_with_explicit_limits`** unchanged; observing the latest verifier query-parameter branches at runtime still follows **rebuild → redeploy → verify** when images predate the change
- **Week 22 closure (Friday task 02):** **`post-week-22-bounded-phase2-recommendation.md`** states the post–week **22** evidence-first default, narrowed plausible follow-ons (week **22** query ergonomics closed unless proven gap), and **anti-recommendations** against momentum-driven reopen—**no** new default implementation lane, **no** phase transition
- **Week 23 (readiness / capability decision-support contract):** **`readiness-capability-decision-support-contract.md`** defines the bounded Phase **2** graph for prerequisites, blockers, planning scopes, and capability-row **`feature`** links; **`schemas/capabilities.py`** adds **`PrerequisiteName`** and explicit **`Field`** descriptions—interpretation-only, **no** workflow authorization or dry-run execution semantics
- **Week 23 (readiness snapshot history query surface):** **`GET /api/v1/readiness-snapshot-history`** exposes bounded **`limit`**, optional **`blocker`** (`ReadinessBlockerName`), **`include_blockers_detail`**, and **`read_side_query`** echo—planning-support inspection of persisted snapshots only; **`read_side_query`** schema extended with **`readiness_blocker_filter_requested`**
- **Week 23 (readiness / capability product navigation):** WebUI **`view=`** URLs carry bounded **`readiness_blocker`**, **`readiness_prerequisite`**, and **`readiness_capability_feature`** query parameters for shareable scroll context on Readiness; Capabilities → Readiness controls, Readiness blocker/prerequisite drilldowns, Overview and Platform Health decision-support summary lines, and workflow-history / audit-history **`HistoryEvidenceDrilldown`** to Readiness are **read-only operator usability**—not API-side workflow filters, not dry-run execution
- **Week 23 (regression and structural verifier):** repository **`pytest`** / **`vitest`** pin prerequisite↔blocker and capability↔blocker link shapes; **`verify-core-runtime.sh`** asserts structural presence of **`related_readiness_blockers`** on capability items and **`related_prerequisites`** on dry-run readiness blockers in **`/api/v1/capabilities`** compact JSON—contract presence only, not business-logic duplication
- **Week 23 (operator docs alignment):** **`roadmap.md`**, **`deployment-runbook.md`**, **`data-flows.md`**, and **`03-CURRENT-STATUS.md`** describe the same week **23** story: Phase **2**–safe **decision-support** navigation and interpretation—**no** workflow progress, **no** dry-run authorization, **no** validation engine; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 23 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **23** as closed bounded readiness/capability decision-support (contract, **`readiness-snapshot-history`** API, WebUI navigation and drilldowns, Overview/Platform Health cues, workflow/audit Readiness drilldown, **`pytest`** / **`vitest`** / structural **`verify-core-runtime`** capabilities checks, cross-doc alignment); **`conditionally_ready_with_explicit_limits`** unchanged; observing the latest capabilities decision-support verifier branches at runtime still follows **rebuild → redeploy → verify** when images predate the change
- **Week 23 closure (Friday task 02):** **`post-week-23-bounded-phase2-recommendation.md`** states the post–week **23** evidence-first default, narrowed plausible follow-ons (week **23** decision-support closed unless proven gap), and **anti-recommendations** against momentum-driven reopen—**no** new default implementation lane, **no** phase transition
- **Week 24 Monday task 01 (change intelligence contract):** **`change-intelligence-contract.md`** plus **`schemas/change_intelligence.py`** define the bounded Phase **2** cross-domain **recent change** summarization vocabulary, explicit non-goals, and safety framing—**aggregation-and-interpretation** over existing read-side evidence (weeks **19–23**), **not** duplicate per-domain comparison semantics; repository **`pytest`** locks literals—**no** new persistence or collector models in that task, **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Monday task 02 (backend change summary aggregation):** **`GET /api/v1/change-intelligence/recent-summary`** aggregates **existing** inventory/topology/policy snapshot table metrics, **`load_sync_runs`**, and readiness snapshot history into **`RecentChangeSummaryResponse`** with per-domain **`RecentChangeDomainSlice`** honesty—**no** validation/drift/safe-to-change/workflow semantics; **`summarize_topology_snapshot_metrics()`** added for parity with inventory/policy snapshot metrics; **`test_app.py`** pins empty and populated states—**read-only Phase 2**, **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Tuesday task 01 (Overview recent change surface):** Overview **`RecentChangeIntelligencePanel`** calls the change-intelligence summary API with bounded copy (explicit non-claims), per-domain headlines, present/partial/absent counts, loading and error paths, **`vitest`** coverage—**not** a Grafana-style dashboard; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Tuesday task 02 (Platform Health change-intelligence cues):** Platform Health adds a **bounded recent change** summary card and **`Recent change (bounded support)`** trust cue from the same **`useRecentChangeSummaryQuery`** as Overview—coarse supporting context only; Overview remains the richer surface; **`countRecentChangeEvidenceStatuses`** helper + **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Wednesday task 01 (Devices, Topology, Policies change linkage):** Overview **`RecentChangeIntelligencePanel`** adds per-domain **Open …** **read-only** **`view=`** navigation to Devices, Topology, and Policies (same **`navigateToEvidenceView`** pattern as history drilldowns); Platform Health adds compact **Open Devices / Topology / Policies** links—lists remain **not** filtered by the aggregation window; honest absence copy; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Wednesday task 02 (Workflow-history and audit-history change-intelligence navigation):** the same panel adds **Open Workflow history** / **Open Audit history**; Workflow History and Audit History pages add **`ChangeIntelligenceOverviewLink`** to **Overview** for the bounded recent-change summary—**bidirectional** read-only interpretation cues, **not** workflow execution or forensic expansion; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Thursday task 01 (change-intelligence tests and verifier):** **`test_app.py`** pins stable **six-domain order**, full **explicit_non_claims** list parity, and **audit_history** **partial** when only sync runs exist; **`recent-change-intelligence-panel.test.tsx`**; **`verify-core-runtime.sh`** structural substring checks for **`/api/v1/change-intelligence/recent-summary`** (contract id, **`window_semantics`**, key domains, **`sync_runs_limit_applied`** echo)—**not** shell-side scoring; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 24 Thursday task 02 (operator docs alignment):** **`roadmap.md`**, **`deployment-runbook.md`**, and **`data-flows.md`** describe the same **week 24** change-intelligence product story as shipped code and the verifier—**no** new authority claims, **no** phase transition language
- **Week 25 Monday task 01 (investigation workspace contract):** **`investigation-workspace-contract.md`** plus **`schemas/investigation_workspace.py`** define the bounded Phase **2** **read-only** investigation-workspace vocabulary—assembly of **existing** evidence (including **change intelligence** as one context source and **platform status** for current posture)—explicit non-goals and bounded suggestion rules; **no** workflow/dry-run/validation semantics; repository **`pytest`** locks literals—**no** new persistence or collector models in that task, **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Monday task 02 (investigation context assembly):** **`GET /api/v1/investigation-workspace/context`** composes nested **existing** responses (change-intelligence **`recent-summary`**, **`/api/v1/platform/status`**, **`/api/v1/capabilities`**) with **`InvestigationContextAssemblyResponse`** and **`services/investigation_workspace.py`**—interpretation support only; **`test_app.py`** + structural **`verify-core-runtime.sh`** checks; **no** new collection, scoring, or workflow semantics; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Tuesday task 01 (Overview investigation entrypoint):** **WebUI** **Overview** **Investigation workspace (bounded)** card + **`view=investigation`** / **Investigation** nav consume **`getInvestigationWorkspaceContext`** with **`sync_runs_limit`** aligned to the Overview recent-change window—read-only bounded copy; **`vitest`** for Overview + Investigation views; **not** workflow, validation, or approval semantics; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Tuesday task 02 (investigation workspace product view):** dedicated **`InvestigationWorkspaceProduct`** layout—safety framing, recent-change domain grid with navigation, platform recovery/read paths/components, capabilities excerpt + matrix preview, reload + hub navigation; shared domain labels with Overview recent-change; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Wednesday task 01 (investigation recency anchors and context panels):** **`InvestigationEvidenceTimeline`** surfaces bounded **recency anchors** from timestamps already embedded in nested payloads only (explicitly **not** a unified forensic or workflow chronology); **`InvestigationContextPanels`** summarize cross-domain posture, change signals, and capability anchors; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Wednesday task 02 (next-inspection suggestions):** **`next_inspection_framing`** / **`next_inspection_suggestions`** on **`InvestigationContextAssemblyResponse`**—deterministic, evidence-derived **navigation** prompts from nested fields (**no** cross-domain scoring or execution authority); **`InvestigationContextDomain`** extended with **`capabilities`** where relevant; WebUI **`InvestigationNextInspection`**; repository **`pytest`** / **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Thursday task 01 (investigation tests and structural verifier):** tighter **`pytest`** / **`vitest`** plus **`verify-core-runtime.sh`** substring checks for investigation JSON shape (including **`framing_rule`**, **`suggestion_id`**, **`context_domain`**, bounded-query **`next_inspection`** presence)—structural presence only, **not** shell duplication of assembly rules; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 Thursday task 02 (operator docs alignment):** **`roadmap.md`**, **`deployment-runbook.md`**, and **`data-flows.md`** describe the same **week 25** investigation-workspace operator story as shipped code and the verifier—read-only assembly, dedicated **`view=investigation`** surface (recency/context UI, next-inspection hints), explicit non-authority language—**no** phase transition, **no** workflow or validation claims; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 25 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **25** as closed bounded **investigation workspace** (contract + **`GET /api/v1/investigation-workspace/context`** assembly + **`next_inspection_*`**, Overview entry + **`InvestigationWorkspaceProduct`** with recency/context/next-inspection UI, **`pytest`** / **`vitest`** / structural **`verify-core-runtime`** investigation JSON checks, cross-doc alignment); **`conditionally_ready_with_explicit_limits`** unchanged; observing the latest investigation assembly and verifier branches at runtime still follows **rebuild → redeploy → verify** when images predate the change
- **Week 25 closure (Friday task 02):** **`post-week-25-bounded-phase2-recommendation.md`** states the post–week **25** evidence-first default, narrowed plausible follow-ons (week **25** investigation workspace closed unless proven gap), and **anti-recommendations** against momentum-driven reopen—**no** new default implementation lane, **no** phase transition
- **Week 26 Monday task 01 (evidence pack contract):** **`evidence-pack-contract.md`** plus **`schemas/evidence_pack.py`** define the bounded Phase **2** **read-only** **operator evidence pack** / situation-room vocabulary—**broader composed** read-side domains (including **`investigation_context`** as a pointer to investigation-workspace assembly, not duplicate routing)—explicit non-goals and safety framing; repository **`pytest`** locks literals—**no** new API route, persistence, or collector models in that task; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Monday task 02 (backend situation-pack assembly):** **`GET /api/v1/evidence-pack/situation`** — **`services/situation_pack.py`** composes **existing** read-side responses (devices, topology, policies, readiness snapshot history, workflow history, audit history, nested **`investigation_context`** investigation assembly)—**no** new collectors, validation verdicts, or cross-domain scoring; repository **`pytest`** pins assembly framing and bounded **`sync_runs_limit`** echo alignment; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Tuesday task 01 (Overview situation-room entrypoint):** WebUI **Overview** **`Situation room (bounded evidence pack)`** card + shell **`view=situation-room`** + **`SituationRoomView`** consuming **`getEvidencePackSituation`**—read-only framing, same **`sync_runs_limit`** as recent-change; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Tuesday task 02 (situation-room product view):** **`SituationRoomProduct`**—pack surface cards for all six top-level domains plus nested investigation (**timeline**, **context panels**, **next-inspection** reuse), full explicit non-claims, assembly notes, nav hub—interpretation-only; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Wednesday task 01 (cross-domain evidence pack sections):** numbered cross-domain sections + bounded gap callouts from pack fields (now **`situation_review_guidance.explicit_missing_evidence_notes`** on the assembly)—**no** forensic/workflow chronology claims; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Wednesday task 02 (bounded review next steps and evidence gaps):** **`situation_review_guidance`** on **`GET /api/v1/evidence-pack/situation`**—**`review_framing`**, **`explicit_missing_evidence_notes`**, sorted **`review_navigation_prompts`** (**`product_view`** navigation only); WebUI **bounded review navigation** section—**not** validation, safe-to-change authority, or workflow execution; repository **`pytest`** / **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Thursday task 01 (evidence pack tests and verifier):** repository **`pytest`** / **`vitest`** tighten situation-pack regression pins (sorted review prompts, gap-line shape, bounded guidance); **`verify-core-runtime.sh`** adds structural checks for **`GET /api/v1/evidence-pack/situation`**—**no** shell duplication of assembly rules; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 Thursday task 02 (evidence-pack docs, roadmap, and runbook alignment):** **`roadmap.md`**, **`deployment-runbook.md`**, and **`data-flows.md`** describe the same week **26** evidence-pack / situation-room operator story as shipped code and the verifier—read-only composed assembly, **`situation_review_guidance`**, Overview **`view=situation-room`** entry, explicit non-authority language—**no** phase transition, **no** workflow or validation claims; **`evidence-pack-contract.md`** cross-links operator docs; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 26 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **26** as closed bounded **operator evidence pack / situation room** (contract + **`GET /api/v1/evidence-pack/situation`** + **`situation_review_guidance`**, Overview entry + **`SituationRoomProduct`**, **`pytest`** / **`vitest`** / structural **`verify-core-runtime`** evidence-pack JSON checks, cross-doc alignment); **`conditionally_ready_with_explicit_limits`** unchanged; observing the latest situation-pack assembly and verifier branches at runtime still follows **rebuild → redeploy → verify** when images predate the change
- **Week 26 closure (Friday task 02):** **`post-week-26-bounded-phase2-recommendation.md`** states the post–week **26** evidence-first default, narrowed plausible follow-ons (week **26** evidence pack / situation room closed unless proven gap), and **anti-recommendations** against momentum-driven reopen—**no** new default implementation lane, **no** phase transition
- **Week 27 Monday task 01 (path-analysis contract and gap audit):** **`path-analysis-contract.md`**, **`ADR-0002`**, and **`schemas/path_analysis.py`** define **`path_analysis_phase2_v1`**—bounded read-only vocabulary (policy-anchored subject, intended/observed hints, candidate-path summaries, evidence attribution, freshness, truth-alignment posture, caveats, explicit non-claims); gap audit documents existing inventory/topology/policy/platform/ODL evidence vs missing dataplane/TE/controller path truth; repository **`pytest`** locks literals—**no** workflow or validation semantics; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 27 Monday task 02 (policy path-analysis read API):** **`GET /api/v1/policies/{policy_id}/path-analysis`** implements **`build_policy_path_analysis_response`** (`services/path_analysis.py`) with **`PathAnalysisViewResponse`**; **404** when the policy id is missing from normalized inventory records; repository **`pytest`** covers success, partial support, unsupported, not-found, persisted-fallback stale row; HTTP metrics via existing middleware—**not** dataplane/TE/controller path truth, **not** workflow or validation semantics; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 27 Monday task 03 (policy details path-analysis WebUI consumption):** **Policies** `view=policies` selected-policy region includes a **Path analysis** panel (`PolicyPathAnalysisPanel`) backed by **`apiClient.getPolicyPathAnalysis`**, typed **`PathAnalysisViewResponse`** in **`contracts.ts`**, **`usePolicyPathAnalysisQuery`**, loading/error/**404**/empty-partial copy, explicit non-claims and caveats; repository **`vitest`**—**not** topology explorer changes, **not** implied dataplane precision; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 27 Tuesday task 01 (related policies from topology objects API):** **`GET /api/v1/topology/objects/{object_id}/related-policies`** — **`TopologyObjectRelatedPoliciesResponse`** maps a topology **`node_id`** or **`link_id`** to policy records whose **`headend` / `endpoint` / `source_target`** match node **`node_id` / `display_name` / `device_id`** (exact strings); links union both endpoints; **`404`** when the id is absent from the current topology snapshot; **`topology-related-policies-contract.md`**; repository **`pytest`**—**not** impact simulation, **not** adjacency-as-dependency truth; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 27 (midweek–Thursday, bounded operator surfaces):** **degraded-policy v1** classification on **`GET /api/v1/policies`** with **WebUI** filter and Overview/Platform Health drill-downs; **topology** and **devices** **related-policies** UI + **policy** **topology-impact** and **path-analysis** panels; **investigation** **`inv_from`** breadcrumbs; **`verify-core-runtime.sh`** week **27** structural sampling when **`python3`** and non-empty policy/topology lists—**read-only Phase 2**, explicit non-claims in contracts; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 27 Friday task 01 (docs alignment for path, impact, degraded surfaces):** **`architecture.md`**, **`data-flows.md`**, **`services.md`**, **`roadmap.md`**, **`dashboards.md`**, and **`platform/README.md`** describe the same shipped week **27** story—path-analysis, topology↔policy naming pivots, degraded-policy v1—with **explicit non-claims** and **Phase 2** boundaries; **no** phase-transition language, **no** workflow or validation-engine claims
- **Week 27 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **27** as closed bounded **path-analysis**, **topology↔policy** pivots, **degraded-policy v1**, **investigation** **`inv_from`**, **`verify-core-runtime.sh`** week **27** structural sampling, and cross-doc alignment; **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-27-friday-task-02-week27-rollup-and-status-update.md`](../../agent/sdn-tasks/completed/week-27-friday-task-02-week27-rollup-and-status-update.md); post–week **27** scheduling posture [`post-week-27-bounded-phase2-recommendation.md`](./post-week-27-bounded-phase2-recommendation.md) ([`week-27-friday-task-03-post-week27-next-bounded-recommendation.md`](../../agent/sdn-tasks/completed/week-27-friday-task-03-post-week27-next-bounded-recommendation.md))
- **Week 28 Monday task 01 (failure-impact contract):** **`failure-impact-contract.md`** defines **`failure_impact_v1`** purpose, evidence bounds, degraded rollup semantics, and explicit non-claims—documentation-only; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Monday task 02 (failure-impact read API):** **`GET /api/v1/topology/objects/{object_id}/failure-impact`** — **`FailureImpactViewResponse`** (**`schemas/failure_impact.py`**, **`services/failure_impact.py`**) aggregates related-policy totals, **`degraded_policy_v1`** breakdown scoped to that set, path-analysis support tallies, caveats, and freshness—**404** when the topology object is unknown (same rules as related-policies); repository **`pytest`**—**not** graph simulation or dependency truth; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Monday task 03 (Topology failure-impact panel):** **WebUI** **`TopologyFailureImpactPanel`** on **`view=topology`** with **`useTopologyFailureImpactQuery`**, explicit bounded copy, loading/error/404/empty/partial states, Policies and Investigation drill-downs; repository **`vitest`**—**not** blast-radius semantics; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Monday task 04 (Investigation entry from failure impact):** **Open in Investigation** from the failure-impact panel sets **`failure_impact_entry=v1`** plus topology object shell params; **`InvestigationNavContextBanner`** shows read-only entry framing—navigation only, no workflow; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Tuesday task 01 (policy evidence timeline contract):** **`policy-evidence-timeline-contract.md`** — **`policy_evidence_timeline_v1`** bounded per-policy chronology-like vocabulary from existing timestamps and evidence (not forensic unified timeline, not validation); documentation-only; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Tuesday task 02 (policy evidence timeline API):** **`GET /api/v1/policies/{policy_id}/evidence-timeline`** — **`PolicyEvidenceTimelineResponse`** (**`schemas/policy_evidence_timeline.py`**, **`services/policy_evidence_timeline.py`**); **404** for unknown inventory **`policy_id`**; honest sparse/partial timelines via **`missing_evidence_notes`**; repository **`pytest`**—**not** unified forensic log or validation authority; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Tuesday task 03 (policy details evidence timeline UI):** **Policies** selected-policy region includes **`PolicyEvidenceTimelinePanel`** (**`policy-evidence-timeline-panel.tsx`**, **`usePolicyEvidenceTimelineQuery`**, **`apiClient.getPolicyEvidenceTimeline`**) with loading/error/**404**/sparse notes, ordered anchors + source labels, read-only links to **Investigation** and **Path analysis**; **`vitest`** in **`policy-evidence-timeline-panel.test.tsx`** and **`policies-view.test.tsx`**—topology-impact and path-analysis panels unchanged; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Tuesday task 04 (workflow/audit drillthrough to policy timeline):** **Workflow history** and **Audit history** expose **`HistoryPolicyEvidenceTimelineDrilldown`** when a row carries **`policy_comparison_to_previous.change_preview`** — navigates to **Policies** with **`policy_id`** + client-only **`policy_evidence_timeline_focus=v1`** (scroll + brief card emphasis); **`vitest`**—existing history drilldown semantics unchanged; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Wednesday task 01 (topology risk summary contract):** **`topology-risk-summary-contract.md`** defines **`topology_risk_summary_v1`**—explicit non-claims; documentation-only; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Wednesday task 02 (topology risk summary API):** **`GET /api/v1/topology/risk-summary`** — **`TopologyRiskSummaryResponse`** (**`services/topology_risk_summary.py`**)—**not** SLA or traffic risk; repository **`pytest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Wednesday task 03 (Overview and Topology risk surfaces):** **`TopologyRiskAttentionPanel`** on **Overview** (bounded rows + drillthrough) and **Topology** (full table + in-page selection)—**`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Wednesday task 04 (investigation seeding from risk summary):** risk-summary rows **Open investigation** with **`risk_summary_entry=v1`** and pinned topology object—navigation-only shell; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Thursday task 01 (policy evidence delta contract):** **`policy-evidence-delta-contract.md`** — **`policy_evidence_delta_v1`** vocabulary; explicit non-claims; documentation-only; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Thursday task 02 (policy evidence delta API):** **`GET /api/v1/policies/{policy_id}/evidence-delta`** — **`PolicyEvidenceDeltaResponse`**—honest **`comparison_status`**; **not** drift truth; repository **`pytest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Thursday task 03 (policy details evidence delta UI):** **`PolicyEvidenceDeltaPanel`** on **Policies**—read-only bounded copy; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Thursday task 04 (verifier and regression for week 28 features):** **`verify-core-runtime.sh`** structural checks for week **27–28** read APIs; repository **`pytest`** / **`vitest`**—**not** shell-side business logic; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Friday task 01 (Overview operator-workspace evolution):** **`OperatorWorkspaceEntry`** on **Overview**—read-only navigation composition into risk attention, failure impact (first node), policy timeline/delta (first policy); client-only URL focus hints; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 Friday task 02 (docs alignment for week 28 read-side features):** **`architecture.md`**, **`data-flows.md`**, **`services.md`**, **`roadmap.md`**, **`dashboards.md`**, and **`platform/README.md`** describe the same shipped week **28** story—failure-impact, policy evidence timeline+delta, topology risk summary, Overview operator-workspace entry—with **explicit non-claims** and **Phase 2** boundaries; **no** phase-transition language; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 28 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **28** as closed bounded **failure-impact v1**, **topology risk summary v1**, **policy evidence timeline+delta v1**, **Overview** **`OperatorWorkspaceEntry`**, investigation shell hints (**`failure_impact_entry`**, **`risk_summary_entry`**), **`verify-core-runtime.sh`** / repository **`pytest`** / **`vitest`** week **28** evidence, and cross-doc alignment; **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-28-friday-task-03-week28-rollup-and-status-update.md`](../../agent/sdn-tasks/completed/week-28-friday-task-03-week28-rollup-and-status-update.md); post–week **28** scheduling posture [`post-week-28-bounded-phase2-recommendation.md`](./post-week-28-bounded-phase2-recommendation.md) ([`week-28-friday-task-04-post-week28-next-bounded-recommendation.md`](../../agent/sdn-tasks/completed/week-28-friday-task-04-post-week28-next-bounded-recommendation.md))
- **Week 29 Monday task 01 (topology object dossier v1 contract):** **`topology-object-dossier-contract.md`** defines **`topology_object_dossier_v1`**—normative composed workspace for a topology **node** or **link** (identity, posture summary, failure-impact, risk-summary contribution, related policies + degraded preview, navigation pivots, freshness/caveats)—**reuse-only** from week **27–28** contracts; explicit non-claims; **no** API or WebUI in this task—documentation-only; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 29 Thursday task 04 (evidence export regression and docs alignment):** repository **`pytest`** / **`vitest`** tighten **`evidence_export_v1`** envelope and UI coverage (investigation **`sync_runs_limit`** echo in **`subject_ref`**, Markdown situation export, **`source_contract_ids`** vs nested **`contract_id`** echoes, **`EvidenceExportActions`** static render); **`verify-core-runtime.sh`** asserts structural substrings on **`/api/v1/exports/...`** (always-on situation + investigation summaries; policy + topology dossier exports when **`python3`** samples ids); **`data-flows.md`** and **`deployment-runbook.md`** describe the same bounded export story as shipped code and the verifier—**not** compliance or tamper-evidence claims; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 29 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **29** as closed bounded **dossiers** (topology + policy), **global operator search**, **evidence export v1**, **NOC cockpit v1** (**`overview_mode`**), repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** evidence (including **app-web** **`/assets/*.js`** **`noc_cockpit_v1`** + **`overview_mode`** markers); **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-29-friday-task-03-week29-regression-hardening-and-rollup.md`](../../agent/sdn-tasks/completed/week-29-friday-task-03-week29-regression-hardening-and-rollup.md); post–week **29** scheduling posture [`post-week-29-bounded-phase2-recommendation.md`](./post-week-29-bounded-phase2-recommendation.md) ([`week-29-friday-task-04-post-week29-next-bounded-recommendation.md`](../../agent/sdn-tasks/completed/week-29-friday-task-04-post-week29-next-bounded-recommendation.md))
- **Week 30 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **30** as closed bounded **cross-domain delta digest**, **operator briefing**, **evidence replay**, **briefing export bundle** (**`briefing_export_bundle_v1`**), **NOC cockpit 2.0** (**strategic pivots**, **evidence replay** entry, **global search** digest/replay pivots), repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** evidence (including **`GET /api/v1/delta-digest`**, **`GET /api/v1/operator-briefing`**, extended **app-web** **`/assets/*.js`** markers); **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-30-friday-task-03-week30-regression-hardening-and-rollup.md`](../../agent/sdn-tasks/completed/week-30-friday-task-03-week30-regression-hardening-and-rollup.md); post–week **30** scheduling posture [`post-week-30-bounded-phase2-recommendation.md`](./post-week-30-bounded-phase2-recommendation.md) ([`week-30-friday-task-04-post-week30-next-bounded-recommendation.md`](../../agent/sdn-tasks/completed/week-30-friday-task-04-post-week30-next-bounded-recommendation.md))
- **Week 31 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **31** as closed bounded **Service Explorer** (**`service_explorer_v1`**), **policy explainability workspace** (**`policy_explainability_workspace_v1`**), **Maintenance Preview** (**`maintenance_preview_v1`**), **Impact Report** (**`impact_report_v1`**, reports **`GET`**s, replay vs export boundary), **NOC cockpit 3.0** (**launch grid**, strategic pivots), **global search** week **31** impact pivots + **Impact report hub**, repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** evidence (including **`GET /api/v1/maintenance-preview`**, **`service_explorer_v1`** / **`policy_explainability_workspace_v1`** / **`Impact report hub`** in **app-web** bundle); **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-31-friday-task-03-week31-regression-hardening-and-rollup.md`](../../agent/sdn-tasks/completed/week-31-friday-task-03-week31-regression-hardening-and-rollup.md); post–week **31** scheduling posture [`post-week-31-bounded-phase2-recommendation.md`](./post-week-31-bounded-phase2-recommendation.md) ([`week-31-friday-task-04-post-week31-next-bounded-recommendation.md`](../../agent/sdn-tasks/completed/week-31-friday-task-04-post-week31-next-bounded-recommendation.md))
- **Week 32 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **32** as closed bounded **Service Dossier v1** (**`service_dossier_v1`**, **`GET /api/v1/services/{service_id}/dossier`**, **`view=service-dossier`**, policy/topology/NOC/search pivots), **Change Safety Case v1** (**`change_safety_case_v1`**, **`GET /api/v1/reports/change-safety-case/...`**, **`view=change-safety-case`**, report download vs **`evidence_export_v1`** replay boundary), **NOC cockpit** + **global search** week **32** Friday integration (**Service dossier**, **Change safety case** policy/maintenance/hub navigators), repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** evidence (**app-web** **`/assets/*.js`** **`service_dossier_v1`** / **`change_safety_case_v1`** markers; **optional** structural compact **`GET`** sampling for dossier and change-safety-case report routes when **`python3`** and sampling gates pass—[`week-32-verifier-parity-contract.md`](./week-32-verifier-parity-contract.md); honest skip when gates fail); **`conditionally_ready_with_explicit_limits`** unchanged; canonical docs rollup [`week-32-friday-task-02-week32-docs-roadmap-rollup-and-posture.md`](../../agent/sdn-tasks/completed/week-32-friday-task-02-week32-docs-roadmap-rollup-and-posture.md); completed-task archive inventory [`week-32-archive-index.md`](../../agent/sdn-tasks/completed/week-32-archive-index.md); post–week **32** scheduling posture [`post-week-32-bounded-phase2-recommendation.md`](./post-week-32-bounded-phase2-recommendation.md)
- **Week 33 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **33** as closed bounded **integrity-and-evidence** lane—**week-32** verifier parity contract + optional runtime **`GET`** sampling, replay/report/download honesty regression, **`week-32-archive-index.md`**, roadmap/status and **`post-week-31`** / **`post-week-32`** alignment, **`week-33-evidence-backed-reassessment-memo.md`**, **`week-33-bounded-next-slice-recommendation.md`** (default next slice: **operator contract labeling**); **no** new **Service Dossier** / **Change Safety Case** product semantics; **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-33-friday-task-02-week33-rollup-docs-closure-and-posture-confirmation.md`](../../agent/sdn-tasks/completed/week-33-friday-task-02-week33-rollup-docs-closure-and-posture-confirmation.md); post–week **32** scheduling and next-slice default [`post-week-32-bounded-phase2-recommendation.md`](./post-week-32-bounded-phase2-recommendation.md), [`week-33-bounded-next-slice-recommendation.md`](./week-33-bounded-next-slice-recommendation.md)
- **Week 34 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **34** as closed bounded **innovation** lane—**Path Explorer v1** (**`path_explorer_v1`**, **`GET /api/v1/path-explorer`**, **`view=path-explorer`**, Policies/NOC/search/overview pivots), **Service Impact Workspace v1** (**`service_impact_workspace_v1`**, **`GET /api/v1/service-impact-workspace`**, **`view=service-impact-workspace`**, export/replay boundary per contracts), repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** evidence (**app-web** **`/assets/*.js`** markers); **`conditionally_ready_with_explicit_limits`** unchanged; canonical docs rollup [`week-34-friday-task-02-week34-docs-roadmap-rollup-and-posture.md`](../../agent/sdn/week-34-friday-task-02-week34-docs-roadmap-rollup-and-posture.md); post–week **34** scheduling posture [`post-week-34-bounded-phase2-recommendation.md`](./post-week-34-bounded-phase2-recommendation.md) (evidence-first **one** slice; week **33** **labeling** default remains available when prioritized)
- **Week 35 Monday task 01 (service evidence timeline v1 contract):** [`service-evidence-timeline-contract.md`](./service-evidence-timeline-contract.md) defines **`service_evidence_timeline_v1`**—bounded **service-primary** chronology vocabulary (same **`service_id`** as Service Explorer), explicit **non-claims**, overlap boundaries vs adjacent surfaces; **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-35-schedule-overview.md`](../../agent/sdn/week-35-schedule-overview.md)
- **Week 35 Monday task 02 (service evidence timeline API + UI):** **`GET /api/v1/services/{service_id}/evidence-timeline`** (**`service_evidence_timeline_v1`**) — membership anchors + per-member **`policy_evidence_timeline_v1`** projections; **WebUI** **Service Explorer** detail + **Service dossier** **`ServiceEvidenceTimelinePanel`**; repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`** when **`first_service_id`** sampled; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 Tuesday task 01 (service evidence delta v1 contract):** [`service-evidence-delta-contract.md`](./service-evidence-delta-contract.md) defines **`service_evidence_delta_v1`**—**service-primary** comparison vs **policy** **`policy_evidence_delta_v1`** (grouped membership, **`degraded_service`**, **`topology_links`**, optional per-member policy-delta **pointers**); explicit **non-claims** (no service drift truth, SLA, cross-service rank); **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-35-schedule-overview.md`](../../agent/sdn/week-35-schedule-overview.md)
- **Week 35 Tuesday task 02 (service evidence delta API + UI):** **`GET /api/v1/services/{service_id}/evidence-delta`** (**`service_evidence_delta_v1`**) — current Explorer detail vs previous persisted policy snapshot anchor; optional per-member **`policy_evidence_delta_v1`** pointers; **WebUI** **Service Explorer** detail + **Service dossier** **`ServiceEvidenceDeltaPanel`**; repository **`pytest`** / **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 Wednesday task 01 (cross-domain evidence consistency summary contract):** [`evidence-consistency-summary-contract.md`](./evidence-consistency-summary-contract.md) defines **`evidence_consistency_summary_v1`** — bounded **alignment / weak alignment / tension** across existing read-side domains; **contradiction taxonomy** and **explicit non-claims** (not validation, drift, root-cause, or workflow safety); **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-35-schedule-overview.md`](../../agent/sdn/week-35-schedule-overview.md)
- **Week 35 Wednesday task 02 (evidence consistency summary API + Overview/NOC):** **`GET /api/v1/evidence-consistency/summary`** — **`evidence_consistency_summary_v1`**; **WebUI** **Overview** + **NOC cockpit** **`EvidenceConsistencyOverviewEntry`**; repository **`pytest`** / **`vitest`** / **`verify-core-runtime.sh`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 Thursday task 01 (evidence consistency workspace + cross-surface pivots):** **WebUI** shell **`view=evidence-consistency`** (**`EvidenceConsistencyView`**) with full **`evidence_consistency_summary_v1`** framing and row pivots; **`navigateToEvidenceConsistencyWorkspace`** from **Overview/NOC**, **Service Explorer** / **dossier**, **Change Safety Case**, **Operator Briefing**, **Path Explorer**, **Service Impact**, **Investigation**; **`vitest`** **`evidence-consistency-view.test.tsx`**, **`evidence-consistency-navigation.test.ts`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 Thursday task 02 (operator contract labeling hardening):** focused WebUI copy on **Explorer vs dossier / impact**, **Change Safety Case vs Impact Report vs export vs replay**, **Path Explorer vs Policies dossier/explainability**; **`deployment-runbook.md`** / **`data-flows.md`** alignment; **`vitest`** **`operator-contract-labeling-anchors.test.ts`** + updated product tests; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 Friday task 01 (verifier, regression, docs alignment):** **`verify-core-runtime.sh`** requires shipped **`/assets/*.js`** substring **`evidence_consistency_summary_v1`** and structural **`GET /api/v1/evidence-consistency/summary`** checks (**`sync_runs_limit_applied`**, **`safety_framing`**); **`deployment-runbook.md`** / **`data-flows.md`** list the same story; **`vitest`** **`week35-verifier-bundle-markers.test.ts`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 35 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **35** as closed bounded **innovation** lane—**service evidence timeline v1** (**`service_evidence_timeline_v1`**), **service evidence delta v1** (**`service_evidence_delta_v1`**), **cross-domain evidence consistency** (**`evidence_consistency_summary_v1`**, summary **`GET`**, **`EvidenceConsistencyView`** + pivots), **operator contract labeling** hardening, **`verify-core-runtime.sh`** / repository **`pytest`** / **`vitest`** week **35** evidence (including **`evidence_consistency_summary_v1`** bundle marker); **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-35-friday-task-02-week35-rollup-status-update-and-next-posture.md`](../../agent/sdn-tasks/completed/week-35-friday-task-02-week35-rollup-status-update-and-next-posture.md); post–week **35** scheduling posture [`post-week-35-bounded-phase2-recommendation.md`](./post-week-35-bounded-phase2-recommendation.md)
- **Week 36 Monday task 01 (topology object evidence timeline v1 contract):** [`topology-object-evidence-timeline-contract.md`](./topology-object-evidence-timeline-contract.md) defines **`topology_object_evidence_timeline_v1`**—bounded **topology-object-primary** chronology (**`node`** \| **`link`**, same **`object_id`** / identity rules as [**Topology object dossier**](./topology-object-dossier-contract.md)); explicit **non-claims**; overlap boundaries vs **policy** / **service** evidence timelines and dossier / failure-impact / risk-summary / investigation / evidence pack; **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-36-schedule-overview.md`](../../agent/sdn/week-36-schedule-overview.md)
- **Week 36 Monday task 02 (topology object evidence timeline API + topology surfaces):** **`GET /api/v1/topology/objects/{object_id}/evidence-timeline`** (**`topology_object_evidence_timeline_v1`**) — topology snapshot / failure-impact / related-policies / risk-summary excerpt + per-related-policy **`policy_evidence_timeline_v1`** projections; **WebUI** **`TopologyObjectEvidenceTimelinePanel`** on **Topology** (selected node/link) and **dossier** workspace; repository **`pytest`** / **`verify-core-runtime.sh`** when **`first_node_id`** sampled; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 36 Tuesday task 01 (topology object evidence delta v1 contract):** [`topology-object-evidence-delta-contract.md`](./topology-object-evidence-delta-contract.md) defines **`topology_object_evidence_delta_v1`**—bounded **topology-object-primary** comparison vs **labeled** persisted anchors (same **`object_id`** rules as dossier); **related-policy set**, failure-impact, risk-summary row, topology row **cues**; explicit **non-claims** (not drift, pairing/coverage truth, blast-radius); reuse **`policy_evidence_delta_v1`** pointers; **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-36-schedule-overview.md`](../../agent/sdn/week-36-schedule-overview.md)
- **Week 36 Tuesday task 02 (topology object evidence delta API + topology/dossier surfaces):** **`GET /api/v1/topology/objects/{object_id}/evidence-delta`** (**`topology_object_evidence_delta_v1`**) — compares current object-scoped assemblies to a **previous** persisted **topology + policy** snapshot pair when policy history allows; **WebUI** **`TopologyObjectEvidenceDeltaPanel`** on **Topology** (selected node/link) and **dossier** workspace; repository **`pytest`** / **`verify-core-runtime.sh`** when **`first_node_id`** sampled; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 36 Wednesday task 01 (maintenance evidence workspace v1 contract):** [`maintenance-evidence-workspace-contract.md`](./maintenance-evidence-workspace-contract.md) defines **`maintenance_evidence_workspace_v1`** — **maintenance-primary** composed workspace over **Maintenance Preview**, **Topology object dossier**, **topology object** evidence timeline/delta (pointers or nested bodies), pivots to **Change Safety Case** / **Service Impact** / reports; explicit **export / report / replay** boundaries; **not** approval, simulation, or blast-radius truth; **`conditionally_ready_with_explicit_limits`** unchanged; schedule [`week-36-schedule-overview.md`](../../agent/sdn/week-36-schedule-overview.md)
- **Week 36 Wednesday task 02 (maintenance evidence workspace API):** **`GET /api/v1/maintenance-evidence-workspace`** (**`maintenance_evidence_workspace_v1`**) — same subject selectors as maintenance preview; nested **`maintenance_preview_v1`**, **`topology_object_dossier_v1`**, **`topology_object_evidence_timeline_v1`**, **`topology_object_evidence_delta_v1`**, **`change_safety_case_v1`** (**`topology_change_safety`**); repository **`pytest`** **`test_maintenance_evidence_workspace.py`**; **`data-flows.md`**; **`conditionally_ready_with_explicit_limits`** unchanged; **WebUI** week **36** Thursday tasks
- **Week 36 Thursday task 01 (maintenance evidence workspace WebUI + cross-surface pivots):** **`view=maintenance-evidence-workspace`** (**`MaintenanceEvidenceWorkspaceView`**, **`navigateToMaintenanceEvidenceWorkspace`**) with pivots from **Topology** dossier, **Maintenance Preview**, **Change Safety Case**, **Service Impact**; **`vitest`** navigation/product tests; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 36 Thursday task 02 (maintenance intelligence cockpit / search / briefing / export framing):** **NOC** / **global operator search** / **operator briefing** add **`maintenance_evidence_workspace_v1`** pivots and honest export framing; **`verify-core-runtime.sh`** **`maintenance_evidence_workspace_v1`** bundle marker; **`vitest`**; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 36 Friday task 01 (verifier, regression, docs alignment):** **`verify-core-runtime.sh`** structural **`GET`** for **`maintenance-evidence-workspace`** when **`first_node_id`** sampled + bundle markers; **`deployment-runbook.md`** / **`data-flows.md`**; **`vitest`** **`week36-verifier-bundle-markers.test.ts`**; **`contracts.ts`** literal **`contract_id`** for topology-object timeline/delta responses; **`conditionally_ready_with_explicit_limits`** unchanged
- **Week 36 Friday task 02 (rollup, status update, next-posture recommendation):** **`03-CURRENT-STATUS.md`** **Week 36 closure (Friday rollup)**; **`roadmap.md`** **Week 36 innovation lane (closed)**; **`post-week-36-bounded-phase2-recommendation.md`**; **`01-CURRENT-PHASE.md`** unchanged
- **Week 36 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **36** as closed bounded **innovation** lane—**topology object evidence timeline v1**, **topology object evidence delta v1**, **maintenance evidence workspace v1** (API + WebUI + pivots), **NOC** / **search** / **briefing** maintenance integration, **`verify-core-runtime.sh`** / **`vitest`** week **36** evidence; **`conditionally_ready_with_explicit_limits`** unchanged; canonical rollup [`week-36-friday-task-02-week36-rollup-status-update-and-next-posture.md`](../../agent/sdn-tasks/completed/week-36-friday-task-02-week36-rollup-status-update-and-next-posture.md); post–week **36** scheduling posture [`post-week-36-bounded-phase2-recommendation.md`](./post-week-36-bounded-phase2-recommendation.md)
- **Week 24 closure (Friday rollup):** **`03-CURRENT-STATUS.md`** records week **24** as closed bounded **change intelligence** (contract + **`recent-summary`** aggregation, Overview **`RecentChangeIntelligencePanel`** and Platform Health cues, **read-only** **`view=`** drilldowns and history↔Overview links, **`pytest`** / **`vitest`** / structural **`verify-core-runtime`** change-intelligence JSON checks, cross-doc alignment); **`conditionally_ready_with_explicit_limits`** unchanged; observing the latest **`recent-summary`** and verifier branches at runtime still follows **rebuild → redeploy → verify** when images predate the change
- **Week 24 closure (Friday task 02):** **`post-week-24-bounded-phase2-recommendation.md`** states the post–week **24** evidence-first default, narrowed plausible follow-ons (week **24** change intelligence closed unless proven gap), and **anti-recommendations** against momentum-driven reopen—**no** new default implementation lane, **no** phase transition
- **Week 19 closure (Friday task 02):** **`topology-truth-depth-review.md`** adds a **Post–Week 19** note: topology **partiality contract** stays **closed** in shipped code; **default next scheduling** remains **`ADR-0001`** **Priority 2** evidence-first reassessment—**not** topology code by default after inventory work
- **Week 18 delivered value (documentation and trust, not scope expansion):** a single honest operator story for history gates, readiness two-clocks, and Grafana placeholder versus real metrics—without authorizing workflow, dry-run, validation, or broader production-operations claims.
- **Unchanged limits (explicit):** remain in **`Phase 2 — read-only product foundation`**; **Grafana** is not the product surface for readiness, capabilities, or change validation; **change-validation** still has **no** change-validation metric families; workflow implementation and phase transition stay **out of scope** until real contracts exist.
- **Operator narrative alignment:** **`deployment-runbook.md`**, **`data-flows.md`**, **`production-readiness-assessment.md`**, and this roadmap describe the **same** bounded envelope: **`conditionally_ready_with_explicit_limits`**, same-workspace recovery only (host-backed data preserved), **product-owned** persisted history (including rich **`/api/v1/devices`** and **`/api/v1/policies`** **`history`**) versus Grafana **current-metrics** and bounded **`inventory_snapshots`** / **`policy_snapshots`** table mirrors on **`app-api`** **`/metrics`**, optional **read-side query ergonomics** (bounded **`read_side_query`** echo; not search or workflow flags), week **23** **readiness/capability decision-support** URL navigation and capabilities JSON cross-link fields (interpretation-only), **week 24** **change intelligence** (**`GET /api/v1/change-intelligence/recent-summary`**, Overview and Platform Health consumption, bounded **read-only** drilldowns to product and history surfaces—**app-api**–owned semantics, not Grafana), **week 25** **investigation workspace** (**`GET /api/v1/investigation-workspace/context`** nested assembly, WebUI **`view=investigation`** one-stop read-only interpretation surface with explicit non-claims—**not** validation, safe-to-change authority, or workflow execution), **week 26** **evidence pack / situation room** (**`GET /api/v1/evidence-pack/situation`** composed read-side assembly including nested **`investigation_context`**, backend **`situation_review_guidance`**, WebUI **`view=situation-room`**—interpretation and navigation only; **not** command authority or cross-domain scoring), **week 28** **failure-impact**, **topology risk summary**, **policy evidence timeline+delta**, and **Overview** **`OperatorWorkspaceEntry`** (read-only product and navigation-only shell hints—**not** Grafana semantics, **not** blast-radius/SLA/drift authority), **week 34** **Path Explorer** (**`path_explorer_v1`**, **`GET /api/v1/path-explorer`**) and **Service Impact Workspace** (**`service_impact_workspace_v1`**, **`GET /api/v1/service-impact-workspace`**, root JSON **not** **`evidence_export_v1`** replay input per contracts), **week 32** **Service Dossier** (**`GET /api/v1/services/{service_id}/dossier`**, **`view=service-dossier`**) and **Change Safety Case** (**`GET /api/v1/reports/change-safety-case/...`**, **`view=change-safety-case`**, report vs **evidence export** / replay boundaries per contracts—**not** dry-run or approval authority); **week 32** verifier evidence: **app-web** bundle markers plus **optional** structural **`GET`** sampling when **`python3`** and list gates pass ([`week-32-verifier-parity-contract.md`](./week-32-verifier-parity-contract.md)); completed-task inventory [`week-32-archive-index.md`](../../agent/sdn-tasks/completed/week-32-archive-index.md); conditional **`verify-core-runtime`** behavior when persisted rows exist or not, **Readiness** evaluation-sample versus **persisted-snapshot** language where relevant, and explicit **no** workflow, **no** dry-run, **no** validation-engine claims

## Updated Phase 2 Checkpoint Assessment

The current evidence supports a stricter checkpoint conclusion after the accepted
week 13 and week 14 work.

Current maturity by area:

- runtime and verification maturity: strong for the current Phase 2 scope, because repo-built images, startup-contract validation, targeted pytest, and the live `verify-core-runtime` plus `verify-odl-auth` flow now prove the bounded runtime, read-path, and roadmap-posture contracts end to end
- identity maturity: sufficient for bounded operations, because persisted and response-level anchors are explicit where real records exist and the accepted week 13 review closed the default capability item-ID lane with a documented no-change outcome
- read-path maturity: mixed, because inventory is strong enough for routine bounded use, topology pairing semantics are now materially stronger after the accepted week 14 work but topology still remains the weakest current live slice due to partial completeness and still-coarse degraded-scope interpretation, and policy is now stronger than the earlier aggregate-only checkpoint because the current lab exposes 4 detail-ready targets and 4 normalized live `static_local` records even though broader policy truth remains intentionally partial
- history maturity: stronger for read-side evidence but still blocked for workflow implementation, because workflow-history and audit-history now include bounded persisted context, anchors, coverage and source-readiness posture, response-level baseline summaries (preserved versus new baseline), and product consumption on the topology, policies, workflow-history, and audit-history pages, plus a same-workspace restart drill that proves preserved-baseline recovery; they still do not provide durable workflow lifecycle or operator-action history
- capability maturity: strong for planning support and current product interpretation, because support state, implementation status, delivery tier, evidence basis, vendor posture, version scope, workflow-readiness interpretation, and blocker posture are now explicit enough for honest bounded use without implying workflow eligibility or Juniper parity
- workflow-prerequisite clarity: still design-strong but implementation-deferred, because the repository has explicit ownership and sequencing guidance for future workflow-owned state while remaining fully in `Phase 2`

Strongest remaining evidence gap (post week 16–20):

- **No single automatic “next implementation” is prescribed.** Week 16 closed recovery consumption, topology coverage history (persist + consume), policy source-readiness history (persist + consume), history baseline summaries on workflow/audit views, and the same-workspace restart drill with preserved-baseline checks. Week 17 tightened persistence attributes, history API tests, topology/policy history **product + verifier + Grafana honesty**, workflow/audit **baseline callouts**, and verifier/drill **baseline_summary** alignment. Week 18 aligned **devices/inventory** history verifier behavior, workflow/audit inventory trust and contracts, **Readiness** evaluation versus persisted snapshot language (product + Grafana), and honest **change-validation** / **vendor** Grafana scaffolds. **Week 19** closed the **ADR-0001** default **inventory / devices persisted history** slice (API, WebUI, verifier, **`inventory_snapshots`** table metrics, cross-doc alignment). **Week 20** closed the **policy-history deepening** slice (expanded **`/api/v1/policies`** **`history`**, WebUI, verifier, **`policy_snapshots`** table metrics, cross-doc alignment). Pairing, partiality decomposition, node-participation cues, coverage history, week **18–20** inventory- and policy-history and verifier parity, readiness language, and those drill/verifier behaviors should stay **closed unless new evidence reopens them**.
- **Next reassessment (Priority 2):** choose the next *narrow* bounded truth-depth or persistence-backed deepening step based on live lab evidence—either a further topology truth cue only if a concrete gain remains beyond inference, endpoint-pairing, collection-posture, node-participation, and coverage-history work, or an equally bounded policy/history step if that has stronger justification. The live Nokia lab currently supports a narrow detail-ready `static_local` slice (`detail_mode=static_policies_when_present`, bounded detail-ready targets and normalized records in the policy-truth review); broader policy families remain off the table without new collector proof.
- **Workflow implementation, dry-run behavior, and workflow-owned storage** remain out of scope for the default next cycle.

Bounded next steps:

- preserve `Phase 2 — read-only product foundation` and the `conditionally_ready_with_explicit_limits` operating boundary
- keep the accepted week 13 identity no-change outcome closed unless a later concrete consumer proves the remaining item-level identity gap matters in practice
- keep week 14 endpoint-pairing, partiality decomposition, and node-participation implementation outcomes closed; do not reopen pairing- or coverage-history consumption work by default
- keep week 16–17 topology-history, policy-history, history-baseline, restart-drill, and related verifier Grafana doc honesty outcomes closed without new evidence
- keep any policy follow-on constrained to proven live Nokia evidence shapes unless new collector evidence independently proves another supported detail-ready policy family
- preserve `Phase 2 — read-only product foundation` until workflow records, workflow-owned APIs, and validation outputs are all real

## Recommendation For The Next Bounded Cycle

Recommendation: preserve the week **16–20** checkpoint narrative (including **week 19** inventory-history API, product, verifier, metrics, and doc alignment, plus **week 20** policy-history API, verifier, metrics, and cross-doc alignment) and continue bounded Phase 2 product deepening. Do not reopen the completed topology-history, policy-history, history-baseline, restart-drill, week 18–20 inventory- and policy-history, readiness-language, or Grafana-scaffold work without new evidence. Prefer the written decision in **`platform/docs/decisions/ADR-0001-next-bounded-truth-depth-slice.md`** before opening broad implementation in topology, policy, or persistence themes.

Interpret that recommendation narrowly.

- keep the project fully in `Phase 2 — read-only product foundation`
- do not reopen item-identity implementation by default
- do not reopen the accepted week 14 endpoint-pairing slice by default
- do not reopen the already-implemented partiality decomposition by default
- do not reopen the week 16 topology-history, policy-history, history-baseline, or restart-drill slices by default
- do not reopen week **22** read-side query ergonomics, URL shareable filters, or bounded history drilldown navigation by default without new evidence
- do not reopen week **23** readiness/capability decision-support (contract linking, snapshot-history inspection API, shareable Readiness URL parameters, capabilities cross-link JSON, structural verifier checks, or cross-doc alignment) by default without new evidence
- do not reopen week **24** bounded change-intelligence (contract vocabulary, **`recent-summary`** aggregation, Overview/Platform Health surfaces, product↔history **read-only** navigation, repository tests, or structural **`verify-core-runtime`** change-intelligence checks) by default without new evidence
- do not reopen week **25** bounded investigation-workspace work (contract + **`GET /api/v1/investigation-workspace/context`** assembly, **`view=investigation`** product surface including recency/context UI and next-inspection hints, repository tests, or structural **`verify-core-runtime`** investigation JSON checks) by default without new evidence—see [`post-week-25-bounded-phase2-recommendation.md`](./post-week-25-bounded-phase2-recommendation.md)
- do not reopen week **26** bounded evidence-pack / situation-room work (contract + **`GET /api/v1/evidence-pack/situation`** + **`situation_review_guidance`**, **`view=situation-room`** **`SituationRoomProduct`**, repository tests, or structural **`verify-core-runtime`** evidence-pack JSON checks) by default without new evidence—see [`post-week-26-bounded-phase2-recommendation.md`](./post-week-26-bounded-phase2-recommendation.md)
- do not reopen week **27** bounded path-analysis, topology↔policy pivots, degraded-policy v1, investigation **`inv_from`**, **`verify-core-runtime.sh`** week **27** sampling, or cross-doc alignment by default without new evidence—see [`post-week-27-bounded-phase2-recommendation.md`](./post-week-27-bounded-phase2-recommendation.md)
- do not reopen week **28** bounded failure-impact, topology risk summary, policy evidence timeline+delta, Overview **`OperatorWorkspaceEntry`**, investigation shell hints (**`failure_impact_entry`**, **`risk_summary_entry`**), **`verify-core-runtime.sh`** week **27–28** structural checks, repository **`pytest`** / **`vitest`** week **28** regressions, or week **28** cross-doc alignment by default without new evidence—see [`post-week-28-bounded-phase2-recommendation.md`](./post-week-28-bounded-phase2-recommendation.md)
- do not reopen week **29** bounded topology/policy **dossiers**, **global operator search**, **evidence export v1**, **NOC cockpit** (**`overview_mode`**, **`noc_cockpit_v1`**), **`verify-core-runtime.sh`** week **29** structural checks (including **app-web** bundle markers), repository **`pytest`** / **`vitest`** week **29** regressions, or week **29** cross-doc alignment by default without new evidence—see [`post-week-29-bounded-phase2-recommendation.md`](./post-week-29-bounded-phase2-recommendation.md)
- do not reopen week **30** bounded **delta digest**, **operator briefing**, **evidence replay**, **briefing export bundle**, **NOC cockpit 2.0** composition, **global search** week **30** pivots, **`verify-core-runtime.sh`** week **30** structural checks (including digest/briefing **`GET`**s and extended **app-web** bundle markers), repository **`pytest`** / **`vitest`** week **30** regressions, or week **30** cross-doc alignment by default without new evidence—see [`post-week-30-bounded-phase2-recommendation.md`](./post-week-30-bounded-phase2-recommendation.md)
- do not reopen week **31** bounded **Service Explorer**, **policy explainability**, **Maintenance Preview**, **Impact Report**, **NOC cockpit 3.0**, **global search** week **31** impact pivots + **Impact report hub**, **`verify-core-runtime.sh`** week **31** structural checks (including **`GET /api/v1/maintenance-preview`**, **`service_explorer_v1`** / **`policy_explainability_workspace_v1`** / **`Impact report hub`** in **app-web** bundle, policy/maintenance impact **`GET`** sampling), repository **`pytest`** / **`vitest`** week **31** regressions, or week **31** cross-doc alignment by default without new evidence—see [`post-week-31-bounded-phase2-recommendation.md`](./post-week-31-bounded-phase2-recommendation.md)
- do not reopen week **32** bounded **Service Dossier v1**, **Change Safety Case v1**, **NOC cockpit** / **global search** week **32** **Service dossier** and **Change safety case** integration, **`verify-core-runtime.sh`** **`service_dossier_v1`** / **`change_safety_case_v1`** bundle markers, **optional** live structural **`GET`** sampling branches when gates pass ([`week-32-verifier-parity-contract.md`](./week-32-verifier-parity-contract.md)), report-route **`GET`** semantics and replay rejection of root **`change_safety_case_v1`**, repository **`pytest`** / **`vitest`** week **32** regressions, or week **32** cross-doc alignment by default without new evidence—see [`post-week-32-bounded-phase2-recommendation.md`](./post-week-32-bounded-phase2-recommendation.md)
- do not reopen week **35** bounded **service evidence timeline** (**`service_evidence_timeline_v1`**, **`GET /api/v1/services/{service_id}/evidence-timeline`**), **service evidence delta** (**`service_evidence_delta_v1`**, **`GET /api/v1/services/{service_id}/evidence-delta`**), **evidence consistency summary** (**`evidence_consistency_summary_v1`**, **`GET /api/v1/evidence-consistency/summary`**, **`EvidenceConsistencyView`**, **Overview** / **NOC** entry), **operator contract labeling** hardening, **`verify-core-runtime.sh`** week **35** bundle + summary **`GET`** structural checks, **`week35-verifier-bundle-markers.test.ts`**, or week **35** cross-doc alignment by default without new evidence—see [`post-week-35-bounded-phase2-recommendation.md`](./post-week-35-bounded-phase2-recommendation.md)
- do not reopen week **36** bounded **topology object evidence timeline** (**`topology_object_evidence_timeline_v1`**), **topology object evidence delta** (**`topology_object_evidence_delta_v1`**), **maintenance evidence workspace** (**`maintenance_evidence_workspace_v1`**, **`GET /api/v1/maintenance-evidence-workspace`**, **`view=maintenance-evidence-workspace`**), **NOC** / **global search** / **operator briefing** maintenance pivots, **`verify-core-runtime.sh`** **`maintenance-evidence-workspace`** structural **`GET`** + **`maintenance_evidence_workspace_v1`** bundle marker, **`week36-verifier-bundle-markers.test.ts`**, or week **36** cross-doc alignment by default without new evidence—see [`post-week-36-bounded-phase2-recommendation.md`](./post-week-36-bounded-phase2-recommendation.md)
- do not start workflow implementation, dry-run implementation, or any phase transition work
- keep the backend as the brain, the WebUI as the product, Grafana as the observability layer, and ODL bounded

Why this is the right next-cycle focus:

- week 16 closed the topology coverage history, policy source-readiness history, workflow-history and audit-history baseline summaries, and same-workspace restart drill; those slices are now implemented and documented
- week 18 closed the devices-history verifier alignment, inventory workflow/audit trust and contract tests, readiness evaluation versus persisted snapshot clarity, and honest Grafana families for change-validation and vendor/adapters observability; documentation now matches shipped behavior
- the accepted week 13 work closed the default identity lane with a documented no-change decision
- the accepted week 14 work closed the endpoint-pairing and single-sided-link coverage gap
- runtime hardening, verification, and the restart drill are now strong enough for the current bounded scope
- the next cycle should stay inside the read-only envelope: keep docs and phase boundaries honest, deepen read-only usefulness without faking completeness, and preserve the service and architecture boundaries already established

The recommended next-cycle slice should stay limited to:

- preserving the week **16–19** checkpoint honestly and the current safe-use boundary first
- recording the next truth-depth slice decision (see **`platform/docs/decisions/ADR-0001-next-bounded-truth-depth-slice.md`**) before broad implementation churn
- continuing bounded Phase 2 product deepening only where a concrete gain justifies code changes

## Phased Roadmap

### Phase 1 — platform skeleton and architecture-first foundation

Focus:

- platform structure
- service READMEs
- topology skeleton
- provisioning skeletons
- backend, collector, frontend, and Postgres scaffolding
- normalized model and schema direction
- architecture docs

Current state:

- foundation complete and preserved as the base for the current phase
- still intentionally bounded on broader lifecycle hardening, live ingestion depth, and workflows, even though the initial runtime packaging and verification slice is now real

### Phase 2 — read-only product foundation

Expected focus:

- collector-to-backend delivery
- read-only inventory and topology APIs
- read-only product pages
- early normalized policy visibility
- more meaningful observability panels

### Phase 3 — bounded ODL-backed enrichment

Expected focus:

- explicit ODL-backed enrichment where useful
- deeper controller-side visibility
- ODL inputs translated through backend-owned boundaries

### Phase 4 — dry-run and validation workflows

Expected focus:

- workflow model
- dry-run APIs
- diff and validation views
- audit relationships

### Phase 5 — first safe bounded action workflow

Expected focus:

- one narrow safe workflow
- strong post-check validation
- rollback where feasible

### Phase 6 — vendor expansion groundwork

Expected focus:

- Juniper adapter structure
- richer capability matrix behavior
- explicit vendor caveats and support handling

## Immediate Next Steps

Based on the current repo state after the week **16–19** checkpoints, the next likely work should be:

1. preserve the current `conditionally_ready_with_explicit_limits` operating boundary and keep all near-term work inside the Phase 2 read-only safe-use envelope
2. keep the week 16 topology-history, policy-history, history-baseline, and restart-drill outcomes closed by default; do not reopen those slices without new evidence
3. keep week 18–19 inventory-history verifier alignment, inventory **`/metrics`** / Grafana table-mirror honesty, readiness Grafana language, and change-validation/vendor dashboard honesty closed by default unless new metrics or APIs justify a deliberate update
4. keep the accepted week 13 identity outcome closed by default; only reopen deterministic or snapshot-scoped readiness/capability item IDs if a concrete later consumer proves the existing anchors insufficient
5. record a **narrow next-slice decision** (see **`platform/docs/decisions/ADR-0001-next-bounded-truth-depth-slice.md`**) before large implementation churn; continue bounded Phase 2 product deepening only where a concrete gain justifies code changes

## Week 32 innovation lane (closed)

Week **32** shipped the planned bounded Phase **2** lane: **Service Dossier v1** and **Change Safety Case v1**, integrated into **NOC cockpit** and **global operator search** without new search corpora or cockpit semantics reopen—see **`03-CURRENT-STATUS.md`** **Week 32** subsections and **Week 32 closure**, plus [`post-week-32-bounded-phase2-recommendation.md`](./post-week-32-bounded-phase2-recommendation.md) for anti-reopen guardrails and evidence-first scheduling.

Delivered focus (read-only, composition-only):

1. **Service Dossier v1** — **`GET /api/v1/services/{service_id}/dossier`**, **`view=service-dossier`**, pivots from policies, explorer, topology, NOC, and search—does **not** replace Service Explorer or policy/topology dossiers.
2. **Change Safety Case v1** — **`GET /api/v1/reports/change-safety-case/...`**, **`view=change-safety-case`**, explicit evidence gaps and non-claims—**not** dry-run, approval, simulation, or actuation authority; distinct from **`evidence_export_v1`** replay inputs.

Posture preserved:

- **`Phase 2 — read-only product foundation`** unchanged
- week **29–31** surfaces remain **closed** unless new evidence requires a narrow overlap—see **post-week-31** and **post-week-32** notes
- backend-owned assembly semantics and WebUI as the operator-facing product

This lane reduced operator fragmentation for service-centric and pre-change interpretation surfaces while staying honest about topology/policy limits and non-workflow Phase **2** semantics.

## Week 33 integrity lane (closed)

Week **33** executed the bounded **integrity-and-evidence** lane: **week-32** runtime-verifier parity ([`week-32-verifier-parity-contract.md`](./week-32-verifier-parity-contract.md)), completed-task archive ([`../../agent/sdn-tasks/completed/week-32-archive-index.md`](../../agent/sdn-tasks/completed/week-32-archive-index.md)), roadmap / **`03-CURRENT-STATUS.md`** alignment, post-week guidance hardening, and [`week-33-evidence-backed-reassessment-memo.md`](./week-33-evidence-backed-reassessment-memo.md). It did **not** reopen **Service Dossier** or **Change Safety Case** product semantics.

**Explicit next bounded slice (after week 33):** [`week-33-bounded-next-slice-recommendation.md`](./week-33-bounded-next-slice-recommendation.md) — default recommendation was **operator contract labeling** (no new assemblies); **week 34** shipped **composed** Path Explorer + Service Impact Workspace instead—see **`Week 34 innovation lane (closed)`** and [`post-week-34-bounded-phase2-recommendation.md`](./post-week-34-bounded-phase2-recommendation.md) for how the labeling slice remains a valid **next** option and for anti-reopen guardrails.

Posture preserved:

- remain in **`Phase 2 — read-only product foundation`**
- preserve explicit non-claims
- treat week **30–32** product semantics as **closed** unless new evidence requires a narrow overlap
- prefer **one** narrow slice per cycle over parallel churn—see **`post-week-32`** and the memos above

## Week 34 innovation lane (closed)

Week **34** shipped the planned bounded Phase **2** lane: **Path Explorer v1** and **Service Impact Workspace v1**, integrated into **NOC cockpit**, **global operator search**, and **Overview** operator-workspace pivots without reopening week **30–33** integrity semantics as product backlog—see **`03-CURRENT-STATUS.md`** **Week 34 closure (Friday rollup)** and [`post-week-34-bounded-phase2-recommendation.md`](./post-week-34-bounded-phase2-recommendation.md) for anti-reopen guardrails and evidence-first scheduling.

Delivered focus (read-only, composition-only):

1. **Path Explorer v1** — **`GET /api/v1/path-explorer`**, **`path_explorer_v1`**, **`view=path-explorer`**—nested reuse from existing policy/path assemblies; **not** dataplane proof or workflow authority.
2. **Service Impact Workspace v1** — **`GET /api/v1/service-impact-workspace`**, **`service_impact_workspace_v1`**, **`view=service-impact-workspace`**—nested **`service_explorer_v1`** and optional **`failure_impact_v1`**; **not** **`evidence_export_v1`** replay input (root JSON rejected); distinct from **Impact Report** and dossier surfaces per contracts.

Posture preserved:

- **`Phase 2 — read-only product foundation`** unchanged
- week **30–33** surfaces remain **closed** unless new evidence requires a narrow overlap—see **`post-week-32`**, **`post-week-34`**, and **week-33-bounded-next-slice**
- backend-owned assembly semantics and WebUI as the operator-facing product

This lane reduced operator fragmentation for policy-path and service-centric composed surfaces while staying honest about topology/policy limits and non-workflow Phase **2** semantics.

## Week 35 innovation lane (closed)

Week **35** shipped the planned bounded Phase **2** lane: **service-centric evidence chronology and delta**, **cross-domain evidence consistency** as an embedded safety layer, **operator contract labeling** hardening, and **verifier / repository / operator-doc** alignment—see **`03-CURRENT-STATUS.md`** **Week 35 closure (Friday rollup)** and [`post-week-35-bounded-phase2-recommendation.md`](./post-week-35-bounded-phase2-recommendation.md) for anti-reopen guardrails and evidence-first scheduling.

Delivered focus (read-only, composition-only):

1. **Service evidence timeline v1** — **`GET /api/v1/services/{service_id}/evidence-timeline`**, **`service_evidence_timeline_v1`**; **Service Explorer** / **Service dossier** **`ServiceEvidenceTimelinePanel`**; **not** incident SLA or workflow order authority.
2. **Service evidence delta v1** — **`GET /api/v1/services/{service_id}/evidence-delta`**, **`service_evidence_delta_v1`**; **`ServiceEvidenceDeltaPanel`**; **not** service drift truth or cross-service rank.
3. **Cross-domain evidence consistency summary v1** — **`GET /api/v1/evidence-consistency/summary`**, **`evidence_consistency_summary_v1`**; **Overview** / **NOC** **`EvidenceConsistencyOverviewEntry`**; shell **`view=evidence-consistency`** (**`EvidenceConsistencyView`**) with cross-surface pivots; **not** validation engine or root-cause authority.
4. **Operator contract labeling hardening** — copy alignment across explorer/dossier/impact, change safety vs impact vs export vs replay, path vs policies (**`operator-contract-labeling-anchors.test.ts`**); **`deployment-runbook.md`** / **`data-flows.md`**.
5. **Verifier / regression / docs (Friday task 01)** — **`verify-core-runtime.sh`** **`evidence_consistency_summary_v1`** in **`/assets/*.js`** plus structural summary **`GET`** checks; **`week35-verifier-bundle-markers.test.ts`**.

Posture preserved:

- **`Phase 2 — read-only product foundation`** unchanged
- week **29–35** surfaces remain **closed** unless new evidence requires a narrow overlap—see **`post-week-32`**, **`post-week-34`**, **`post-week-35`**, **`post-week-36`**
- backend-owned assembly semantics and WebUI as the operator-facing product

This lane advanced service-first time/delta storytelling and explicit contradiction surfacing without workflow, dry-run, or validation semantics.

## Week 36 innovation lane (closed)

Week **36** shipped the planned bounded Phase **2** lane: **topology object evidence timeline v1**, **topology object evidence delta v1**, **maintenance evidence workspace v1** (read API + WebUI + cross-surface pivots), **NOC** / **global operator search** / **operator briefing** maintenance-evidence integration, and **verifier / repository / operator-doc** alignment—see **`03-CURRENT-STATUS.md`** **Week 36 closure (Friday rollup)** and [`post-week-36-bounded-phase2-recommendation.md`](./post-week-36-bounded-phase2-recommendation.md) for anti-reopen guardrails and evidence-first scheduling.

Delivered focus (read-only, composition-only):

1. **Topology object evidence timeline v1** — **`GET /api/v1/topology/objects/{object_id}/evidence-timeline`**, **`topology_object_evidence_timeline_v1`**; **`TopologyObjectEvidenceTimelinePanel`** on **Topology** + dossier; **not** forensic log or pairing/coverage truth authority.
2. **Topology object evidence delta v1** — **`GET /api/v1/topology/objects/{object_id}/evidence-delta`**, **`topology_object_evidence_delta_v1`**; **`TopologyObjectEvidenceDeltaPanel`**; **not** topology drift truth or blast-radius simulation.
3. **Maintenance evidence workspace v1** — **`GET /api/v1/maintenance-evidence-workspace`**, **`view=maintenance-evidence-workspace`**, nested **`maintenance_preview_v1`** / dossier / timeline / delta / **`change_safety_case_v1`** (**`topology_change_safety`**); **not** **`evidence_export_v1`** root, **not** approval or simulation truth.
4. **Maintenance intelligence surfaces** — **NOC** / **global search** / **briefing** pivots + export honesty per contracts; **`verify-core-runtime.sh`** **`maintenance_evidence_workspace_v1`** in bundle.
5. **Verifier / regression (Friday task 01)** — **`verify-core-runtime.sh`** maintenance-evidence-workspace **`GET`** + topology-object structural **`GET`**s when gates pass; **`week36-verifier-bundle-markers.test.ts`**.

Posture preserved:

- **`Phase 2 — read-only product foundation`** unchanged
- week **29–36** product surfaces remain **closed** unless new evidence requires a narrow overlap—see **`post-week-32`**, **`post-week-34`**, **`post-week-35`**, **`post-week-36`**
- backend-owned assembly semantics and WebUI as the operator-facing product

This lane made **topology objects** and **maintenance subjects** first-class evidence surfaces over time and composition without workflow, dry-run, or validation semantics.

## Post–week 36 scheduling posture (not a default lane)

There is **no** automatic next implementation lane. Prefer **one** evidence-first slice per cycle—see [`post-week-36-bounded-phase2-recommendation.md`](./post-week-36-bounded-phase2-recommendation.md).

## Planned Week 37 bounded lane

Week **37** is planned as a single bounded Phase **2** innovation lane: **operational stability intelligence**.

Planned focus (scheduled, not yet shipped):

1. **Operational stability summary v1** — a new backend-owned summary over existing read-side evidence so operators can see quiet vs churn-heavy vs evidence-weak posture without prediction or approval semantics.
2. **Topology object stability profile v1** — a topology-subject stability surface reusing dossier, failure-impact, timeline, delta, and maintenance-adjacent evidence without reopening week **36** semantics.
3. **Service stability profile v1** — a service-subject stability surface reusing Service Explorer, Service Dossier, and service timeline/delta without reopening week **35** semantics.
4. **Stability workspace and pivots** — a WebUI operator surface that connects the summary and profiles to Overview / NOC, topology, service, maintenance, and change-support surfaces.

Planning posture preserved:

- **`Phase 2 — read-only product foundation`** unchanged
- weeks **35–36** remain **closed** unless new evidence justifies narrow overlap
- no workflow, dry-run, validation, approval, prediction authority, or Grafana-owned product semantics

Primary planning artifacts:

- [`../../agent/sdn/week-37-schedule-overview.md`](../../agent/sdn/week-37-schedule-overview.md)
- [`../../agent/sdn/week-37-monday-task-01-operational-stability-summary-contract.md`](../../agent/sdn/week-37-monday-task-01-operational-stability-summary-contract.md)
- [`../../agent/sdn/week-37-monday-task-02-operational-stability-summary-api-and-tests.md`](../../agent/sdn/week-37-monday-task-02-operational-stability-summary-api-and-tests.md)
- [`../../agent/sdn/week-37-tuesday-task-01-topology-object-stability-profile-contract.md`](../../agent/sdn/week-37-tuesday-task-01-topology-object-stability-profile-contract.md)
- [`../../agent/sdn/week-37-tuesday-task-02-topology-object-stability-profile-api-and-tests.md`](../../agent/sdn/week-37-tuesday-task-02-topology-object-stability-profile-api-and-tests.md)
- [`../../agent/sdn/week-37-wednesday-task-01-service-stability-profile-contract.md`](../../agent/sdn/week-37-wednesday-task-01-service-stability-profile-contract.md)
- [`../../agent/sdn/week-37-wednesday-task-02-service-stability-profile-api-and-tests.md`](../../agent/sdn/week-37-wednesday-task-02-service-stability-profile-api-and-tests.md)
- [`../../agent/sdn/week-37-thursday-task-01-stability-workspace-webui-and-typed-client-consumption.md`](../../agent/sdn/week-37-thursday-task-01-stability-workspace-webui-and-typed-client-consumption.md)
- [`../../agent/sdn/week-37-thursday-task-02-stability-workspace-cross-surface-pivots.md`](../../agent/sdn/week-37-thursday-task-02-stability-workspace-cross-surface-pivots.md)
- [`../../agent/sdn/week-37-friday-task-01-week37-verifier-regression-and-docs-alignment.md`](../../agent/sdn/week-37-friday-task-01-week37-verifier-regression-and-docs-alignment.md)
- [`../../agent/sdn/week-37-friday-task-02-week37-rollup-roadmap-and-next-posture.md`](../../agent/sdn/week-37-friday-task-02-week37-rollup-roadmap-and-next-posture.md)

## Boundary Reminder

Phase transitions should not happen just because files exist.

They should happen only when the foundational architecture is both present and honest enough to support the next class of work without drift.

The project now supports stricter future dry-run planning assessment, but it should still remain fully in `Phase 2 — read-only product foundation` until actual workflow-model, dry-run API, preview or diff, validation-result, and workflow-audit relationship contracts become real.
