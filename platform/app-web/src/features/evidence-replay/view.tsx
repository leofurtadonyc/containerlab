import { EvidenceReplayProduct } from "./evidence-replay-product";

export function EvidenceReplayView() {
  return (
    <section className="evidence-replay-route">
      <EvidenceReplayProduct />
    </section>
  );
}
